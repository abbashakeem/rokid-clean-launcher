<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- phone link glyph fallback: adb shell pm grant com.abbas.hudlauncher android.permission.BLUETOOTH_CONNECT -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- our own BLE server for the iPhone app: adb shell pm grant com.abbas.hudlauncher android.permission.BLUETOOTH_ADVERTISE -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!--
      This firmware reports SDK 32 but does not define Android 12's Bluetooth runtime permissions
      ("Unknown permission: BLUETOOTH_SCAN"), so it enforces the LEGACY model. There, BLE scanning
      silently returns zero results without location permission, and location services must be on.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- this Rokid build enforces the legacy permission for the advertiser -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- brightness picker; granted once via: adb shell appops set com.abbas.hudlauncher WRITE_SETTINGS allow -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <!-- return watcher: adb shell appops set com.abbas.hudlauncher GET_USAGE_STATS allow ; ... SYSTEM_ALERT_WINDOW allow -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- assistant capture: adb shell pm grant com.abbas.hudlauncher android.permission.RECORD_AUDIO -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- self-update: adb shell appops set com.abbas.hudlauncher REQUEST_INSTALL_PACKAGES allow -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Package visibility (Android 11+): lets the app picker see other launchable apps -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".HudApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="${cleartext}"
        android:theme="@style/Theme.Hud">

        <!--
          HOME + DEFAULT categories make the system offer this activity as a launcher.
          stateNotNeeded / excludeFromRecents mirror what stock launchers declare.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:excludeFromRecents="true"
            android:clearTaskOnLaunch="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Grants access to media sessions for the now-playing pill:
             adb shell cmd notification allow_listener com.abbas.hudlauncher/.HudNotificationListener -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- display-off only (system lock action), no key filtering; enable with:
             adb shell settings put secure enabled_accessibility_services com.abbas.hudlauncher/.HudLockService
             adb shell settings put secure accessibility_enabled 1 -->
        <service android:name=".ble.BleScanService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name=".HudLockService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice" android:resource="@xml/lock_service" />
        </service>

        <service
            android:name=".HudNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
