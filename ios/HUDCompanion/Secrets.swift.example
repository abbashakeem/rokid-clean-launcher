// Copy to Secrets.swift and fill in. Secrets.swift is git-ignored.
// Same values the launcher reads from local.properties (HUD_BASE_URL / HUD_API_KEY).
enum Secrets {
    static let backendBaseURL = "https://rokid-hud-backend.onrender.com"
    static let backendAPIKey = "change-me"
}
